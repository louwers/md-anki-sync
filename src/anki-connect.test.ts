import { expect, test } from "vitest";
import { findNotes, notesInfo } from "./anki-connect";

// test("", async () => {
//   const ankiIds = await findNotes("3c18b5af1e4076ef897e0cc4ba066ead");
//   const res = await notesInfo(ankiIds[0]);
//   console.log(res);
//   expect(false).toBe(true);
// })
